# Copy to .env and fill in. Used by: docker compose up

# Database (compose uses these in ConnectionStrings__DockiUpDatabase)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme_use_strong_password
POSTGRES_DB=dockiupdb

# Path inside the API container where project files are stored (named volume mount)
PROJECTS_PATH=/app/projects

# Optional: Docker socket. Leave empty to use default (/var/run/docker.sock when mounted).
DOCKER_SOCKET_PATH=

# Optional: JWT secret if you add auth later
JWT_SECRET_KEY=

# CORS: comma-separated allowed origins (production). Default in compose: http://localhost:4200
# Example: https://app.dockiup.example,https://dockiup.example,http://localhost:4200
CORS_ALLOWED_ORIGINS=http://localhost:4200
